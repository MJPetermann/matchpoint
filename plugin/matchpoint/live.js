import { readFileSync } from "node:fs"
export function startLive(spInterface, callback) {
    // spInterface.rcon(readFileSync("./plugins/matchpoint/cfg/live.cfg", "utf8").split("\n"))
}

export function cancelLive(spInterface) {

}

export function reactivateLive(spInterface) {

}